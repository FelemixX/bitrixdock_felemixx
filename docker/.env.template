# Project name to separate multiple bitrixdock instances
COMPOSE_PROJECT_NAME=bitrixdock

# PHP version settings. Allowed values are: php82, php83, php84, php85
PHP_VERSION=php82

#db version
MYSQL_VERSION=8.4

# Web server type. Allowed value is apache or nginx.
WEB_SERVER_TYPE=nginx

#xhprof sugar crm based viewer
XHPROF_VIEWER_PORT=8888

# MySQL settings
DB_SERVER_TYPE=mysql
MYSQL_DATABASE=bitrix
MYSQL_USER=bitrix
MYSQL_PASSWORD=123
MYSQL_ROOT_PASSWORD=123

# Network
INTERFACE=0.0.0.0

# Site path ./www to store it on top level
SITE_PATH=../www

